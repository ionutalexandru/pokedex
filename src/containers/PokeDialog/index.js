import React, {Component} from 'react'

import {PokeDialog} from '/components/PokeDialog'

export default class PokeDialog extends Component {
  render() {
    return(
      <PokeDialog />
    )
  }
}
